# Chunked Build — GEO Query → Content Brief Agent

Each chunk: red → green → refactor → commit. Vertical-slice first (chunks 1–8 ship a working v1); stretches additive (9–19).

## Vertical slice
- [x] **Chunk 1 — Tracer bullet**: project skeleton, `Skill` base, `define_icp`, placeholder `draft_content_brief`, episodic memory, `RunBudget`, Typer CLI, E2E smoke test. README outline + ARCHITECTURE skeleton committed. 19/19 tests green, ruff clean.
- [x] **Chunk 2** — `generate_geo_query_list` (Haiku, single prompt, 5 buyer framings). 23/23 tests green, ruff clean. Placeholder priority-query picker until ch3 lands.
- [x] **Chunk 3** — `score_queries` (LLM-only) + `select_priority_query` (strategic, not argmax). 27/27 green, ruff clean. Placeholder priority-query picker retired.
- [x] **Chunk 4** — `tools/web_search.py` + `analyze_serp` (snippets only). 30/30 green. Tool returns `list[SerpResult]`; skill synthesizes common angles + content gaps.
- [x] **Chunk 5** — `draft_content_brief` is now SERP-informed. Inputs include `SerpAnalysis` (optional for backward-compat); prompt explicitly requires the angle to exploit a `content_gap` and call out a `common_angle` to differentiate from. 31/31 green.
- [x] **Chunk 6** — Deterministic + model-graded evals + inner-loop revision. Each skill owns its evaluator list; failures trigger re-run with revision header prepended (no cache_control so revisions are cheap). 49/49 green.
- [x] **Chunk 7** — `memory/semantic.py` (sqlite-vec + fastembed, 384-dim BGE-small). Drafter retrieves top-3 similar past briefs as "don't repeat" RAG context. 52/52 green. Note: requires SQLite with extension loading — Brew Python 3.13+ or Docker.
- [x] **Chunk 8** — V1 pipeline validated on 3 real runs (Notion / Linear / Glossier across B2B SaaS + DTC beauty), $0.99 total. Fixes landed: bump per-skill max_output_tokens to prevent mid-JSON truncation; defensive `_coerce_json_list` validators on contracts (model occasionally double-encodes nested arrays); `env_ignore_empty=True` so harness shell can't shadow `.env`; advisory vs blocking eval distinction (model-graded judges no longer gate the run on borderline calls).

## Stretches (additive, no regression to v1)
- [x] **Chunk 9** — `research_company` (Sonnet, 11-section CASINO dossier) runs first; `define_icp` consumes it as upstream grounding. Pydantic `_coerce_json_dict` validator added for SWOT/Porter dict fields (parallel to `_coerce_json_list`). 53/53 green.
- [x] **Chunk 10** — `tools/web_fetch.py` (SSRF-hardened: DNS-resolution check, scheme whitelist, redirect re-validation, 5MB streaming cap, 10s timeout). `agent.py` fetches top-3 SERP pages and populates `SerpResult.extracted_content`. analyze_serp prompt updated to consume page content when present. 62/62 green.
- [x] **Chunk 11** — `tools/dataforseo.py` (hybrid; returns {} without creds, uses Labs API when set) → `score_queries` receives `keyword_metrics` and copies real volume/KD/CPC into each `ScoredQuery.metrics`. 67/67 green. Competitor URLs intentionally deferred (would require 25 SERP calls; current `web_search` for the priority query is sufficient).
- [x] **Chunk 12** — `tools/sitemap_parser.py` (SSRF-hardened, supports `<urlset>` + `<sitemapindex>` recursion, caps at 500). CLI gains `--sitemap URL`. Drafter prompt picks 3-5 real URLs from the sitemap with anchors + placement rationale; leaves the section empty when no sitemap is supplied. 73/73 green.
- [x] **Chunk 13** — Golden regression set (3 curated inputs in `tests/golden/inputs.json`) + `geoquery eval-golden [--report]` command. Haiku-based judge checks ICP role keywords + brief angle keywords (60% threshold). `--report` writes a self-contained HTML report to `eval_reports/<timestamp>.html`. Exits non-zero if pass rate < 80%. 77/77 green.
- [x] **Chunk 14** — `geoquery feedback <run-id> --edited path.md` subcommand. Async outer loop: diffs the edited brief against the original, captures the unified diff into `human_edits`, and (if the Angle line changed) re-embeds the new angle into semantic memory so future similar runs surface it via RAG. 81/81 green.
- [x] **Chunk 15** — Streaming + progress events. `agent.run_brief` takes `on_progress`; CLI passes a stderr echo so the user sees `→ skill / ✓ cost` per stage. `DraftContentBrief.streams = True` switches the drafter to `messages.stream()`, calling `progress_callback(chars_streamed)` per delta. Fake test client gained a minimal `stream()` context. 82/82 green.
- [x] **Chunk 16** — `mcp_server.py` exposes `generate_brief(company, market, sitemap?)` over MCP stdio (FastMCP). Same `run_brief` under the hood. Configure Claude Desktop / Code via `claude_desktop_config.json` — example in module docstring. 84/84 green.
- [x] **Chunk 17** — Dockerfile (python:3.13-slim, bakes the fastembed model in for fast first-run) + docker-compose (mounts ./briefs and ./data, .env passthrough) + `.pre-commit-config.yaml` (ruff check + ruff format + pytest). One-time `ruff format .` pass cleaned 11 files. 84/84 still green.
- [x] **Chunk 18** — 5 more example briefs generated (Stripe / Webflow / HubSpot / Vercel / Patagonia), 8 total across B2B SaaS, fintech, no-code, frontend cloud, DTC beauty, DTC outdoor. Total real-API spend: $3.85. Fixed `analyze_serp` token cap (4k → 6k now that page content lands in the prompt) + hardened `_coerce_json_dict/list` to tolerate model wrappers like `dict({...})`.
- [x] **Chunk 19** — Final pass on README (live progress demo block + 8-brief table + Docker quickstart + MCP setup) and ARCHITECTURE (full per-layer write-ups + decision log with every architecturally significant call). 84/84 still green, ruff clean + formatted.

---

**v1.0 ships.** 19 chunks, 19 commits + 1 doc-only DataForSEO flip, 84 tests, 8 example briefs ($3.85 spent), 6 architectural layers populated end-to-end.

---

# v2: Eval Framework — Production-Grade Trust Layer

Plan: see `/Users/lukaszstrycharz/.claude/plans/here-is-a-new-synchronous-wirth.md` (v2 section).

## Vertical slice
- [x] **Chunk 1 — Tracer bullet (eval framework).** Extracted 2 inline judge prompts to `evals/rubrics/{buyer_realism,brief_specificity}.md`; added `evals/rubric_loader.py` (deep module, single `load_rubric` API, KeyError on missing placeholder); added `BrandVoiceMatchJudge` + `brand_voice_match.md` rubric, wired into `DraftContentBrief.make_evaluators(inputs)` when dossier is present (drafter now takes optional `company_dossier`, threaded through `agent.py`); changed `Skill.make_evaluators` signature `()` → `(inputs)` across 6 skill overrides + 3 test monkeypatches; built `dashboard/data.py::recent_runs()` (parameterized SQL, no Streamlit imports) + `dashboard/app.py` Streamlit entrypoint with env-overridable DB path; added `dashboard` extra to `pyproject.toml` (streamlit==1.40.2, pandas==2.2.3); seeded `EVALS.md` with the four-layer overview + rubric catalog. 100/100 tests green (84 v1 + 5 rubric_loader + 3 brand_voice + 5 dashboard_data + 3 e2e cassette unchanged in count).
- [x] **Chunk 2** — judges #4 (`SearchIntentAlignmentJudge`, takes `Priority` at construction) and #5 (`BriefActionabilityJudge`, no upstream context). Rubric files in `evals/rubrics/`; cassette tests (6 new). Drafter `make_evaluators` attaches actionability always and search-intent when `DraftBriefInputs.priority` is set; `agent.py` now threads the `Priority` through. E2E pipeline cassettes extended for both. EVALS.md catalog updated. 103/103 green.
- [x] **Chunk 3** — Regression scaffolding (smoke tier). `evals/regression.py` ships: `cassette_key()` (sha256 over normalized system + user + model — extracts only `text` fields from multi-block system, ignores cache_control), `RegressionCassetteClient` (load-from-disk replay with SDK-shaped `messages.create` + `messages.stream`), `RegressionStaleCassetteError` (loud on miss with model + 80-char previews + re-record hint), `RecordingCassetteClient` (wraps a real or fake inner client, captures every key→response pair, `.dump(path)` persists). Bootstrap test `tests/test_regression_record.py` (marker `regression_record`, skipped by default) records `notion_b2b_saas` from FakeAnthropicClient + writes `regression_dataset/<slug>/{input,cassette,expected}.json`. Replay test `tests/test_regression.py` (marker `regression_smoke`) parametrizes over `regression_dataset/*` and asserts no skill flips pass↔fail vs baseline. Pyproject markers `regression_smoke` + `regression_full` + `regression_record` added; default `pytest` deselects `regression_record`. `EVALS.md` documents add-a-case + re-record workflows + the cassette-format invariants (outer sorted, inner preserved — Pydantic's `dict` field serialization preserves insertion order). Caught one real bug along the way: initial `sort_keys=True` dump silently broke determinism because Pydantic preserves inner-dict order — fixed and added regression test. 16 new tests (12 module + 4 regression suite), 120 green total, ruff clean.

## Stretches (additive, no v1 regression)
- [x] **Chunk 4** — Expand regression to 30+ cases (full tier). Path 1 (commit `4bd68f2`): added 29 bootstrap cases across e-commerce / fintech / healthtech / edtech / devtools / marketplaces / DTC verticals / design tools, split harness into `regression_smoke` (5 cases, pre-commit, ~7s) + `regression_full` (25 cases, opt-in, ~32s); tier stamped into `expected.json` at record time and used to filter the parametrized replay. Default `pytest` includes smoke, excludes full + record. New harness sanity tests: smoke/full disjoint, total ≥30, each slug has a unique cassette hash set. Path 2 (commit `0be0781`): live-recording infrastructure — `record_case_to_disk()` helper in `evals/regression.py`, `tests/test_regression_record_live.py` with `regression_record_live` marker (deselected by default; requires real `ANTHROPIC_API_KEY`; per-case cap $1.50 in `live_settings` fixture); EVALS.md documents the three subset patterns. **Live recording run #1 (~$2.45 spent, commit `<next>`)**: 3 of 5 smoke cassettes upgraded to `source: "live-anthropic"` (Notion / Stripe / Webflow). 2 failed mid-pipeline (Linear / Glossier) when real Sonnet returned `SerpAnalysis.top_results` as a Python-repr-style string the existing `_coerce_json_list` validator couldn't decode — spawned as a separate task to harden the validator + re-record those two. Replay across the 3 live + 2 still-bootstrap cassettes is green.
- [x] **Chunk 5** — GitHub Actions regression gate. `.github/workflows/regression.yml` runs default `pytest` (smoke tier inlined via `addopts`) + `pytest -m regression_full` on every PR + push to main; concurrency group cancels superseded runs. `scripts/regression_summary.py` parses smoke+full JUnit XML into a Markdown table with per-tier pass/fail counts and a `<details>` block listing failed cases with truncated messages and re-record instructions. Workflow updates a single sticky PR comment (marker `<!-- regression-gate-summary -->`) rather than spamming on every push. Summary also writes to the workflow's `$GITHUB_STEP_SUMMARY` so the run page shows the breakdown inline. Pre-commit config gains a clarifying comment that `pytest -q` covers the smoke tier locally. README adds a "Regression gate" section + branch-protection setup steps. 8 new unit tests for the summary script (junit parsing, formatting, pass/fail rendering, file-missing graceful degradation).
- [x] **Chunk 6** — Streamlit dashboard pages: Evals + Costs + Tools (Drift in chunk 8, Review_Queue in chunk 7). Three new pure-fn helpers in `dashboard/data.py` (`pass_rate_per_skill_per_day`, `cost_per_run`, `skill_failure_rate`), each parameterized SQL + unit-tested. Streamlit pages live under `dashboard/pages/{1_Evals,2_Costs,3_Tools}.py` — thin presentation only, all SQL via `data.py`. `dashboard.app.resolve_db_path` promoted from private. New `scripts/build_demo_db.py` seeds `data/episodic.demo.db` (33 runs across 14 days, deterministic seed=42, anonymized public-company data) so Streamlit Cloud has populated charts on first load. `.gitignore` reworked to commit `data/episodic.demo.db` while keeping `data/episodic.db` (real prod) ignored. 3 new tests for the data helpers (8 total in test_dashboard_data.py). 141 tests green, ruff clean.
- [x] **Chunk 7** — Production sample stream + `human_reviews` table. Schema migration is additive (`CREATE TABLE IF NOT EXISTS human_reviews` + 2 indexes); existing prod DBs auto-migrate on next `EpisodicMemory()` construction. New helpers on EpisodicMemory: `start_human_review` / `record_human_review` / `get_pending_reviews` / `get_review`. `evals/production.py::maybe_sample_for_review` rolls a `random()` die at `settings.sample_rate` (default 10%, env: `SAMPLE_RATE`); accepts a `random.Random` or zero-arg callable for deterministic test behavior. `agent.run_brief` calls the sampler on completion. `dashboard/data.py` gains `pending_reviews` + `judge_outcomes_for_run`. `dashboard/pages/5_Review_Queue.py` shows a select-one-at-a-time queue with the brief inline (expander), the judges' run-time verdicts in a table, and a form (overall 1-5 + 3 per-dimension sliders + notes). Submit writes back via parameterized SQL and reruns. Demo DB rebuilt with a seeded pending review (Figma) + 1 reviewed-low-rating row so the divergence detector lands in chunk 8 with non-empty data. 9 new tests (sampler distribution + table CRUD), 150 total green.
- [x] **Chunk 8** — Drift detection + judge-human divergence + Slack alert + regression-gate demo. `evals/production.py::compute_drift_windows` runs a single CTE-based grouped aggregate (last 7d vs prior 7d per skill); `drift_detected = delta ≤ -10% AND both windows have ≥5 samples` so small-N noise can't fire alerts. `compute_judge_human_divergence` joins reviewed `human_reviews` against `skill_invocations` consensus; flags rubric-recalibration candidates when divergence rate >5%. `post_drift_alert_to_slack` is a tiny webhook POST that no-ops on empty webhook url and accepts a `_httpx_module` test seam. `dashboard/pages/4_Drift.py` renders the per-skill window table, a sorted bar chart of deltas, and a 4-metric judge-vs-human divergence panel that warns when divergence > 5%. `dashboard/app.py` landing page now shows a global drift banner above the runs table (lazy import + try/except keeps the dashboard up even on a fresh data dir). `docs/regression_gate_demo.md` ships the 5-step procedure for capturing the GIF (force `score_queries.composite=5.0` → `BriefSpecificityJudge` flips on the 3 live cassettes → 2 bootstrap cassettes pass-through unchanged → revert → green). 9 new drift tests (4 windowing + 2 divergence + 3 Slack), 159 total green, ruff clean.
- [x] **Chunk 9** — Final docs. EVALS.md filled in: full dashboard usage table (6 pages × what-they-show × built-from), production sample stream + judge-recalibration loop section explaining the human-rating + divergence signal → manual rubric edit → cassette re-record cycle, drift detection section with threshold semantics + Slack alert wire-in, regression-gate demo section pointing at the docs/ procedure. `docs/streamlit_cloud_deploy.md` — 6-step manual deploy + common gotchas + how to refresh the committed demo DB. `docs/linkedin_post.md` — three lengths (200-char hook, 800-char framework, 2000-char framework+proof) so the user can pick. Ship-ready.

---

**v2.0 ships.** 9 chunks, 11 commits (chunks 4 took 3 for path-1 + path-2-infra + path-2-live), ~$2.45 real-API spend on live cassettes, 159+ tests, 30 regression cases (3 live + 27 bootstrap), 5 dashboard pages, full CI regression gate, production sample stream, drift detection, judge-vs-human divergence loop. v1.0 untouched except for the additive `make_evaluators(inputs)` API change + the `human_reviews` schema migration.

---

# v3: Self-Improving Loops

Plan: see `/Users/lukaszstrycharz/.claude/plans/here-is-a-new-synchronous-wirth.md` (v3 section).

## Vertical slice
- [x] **Chunk 1 — Score-aware memory (tracer bullet).** `memory/semantic.py`: additive ALTER-TABLE migration adds `eval_score` / `eval_details_json` / `section_skeleton` to the `briefs` table (pre-v3 semantic.db files migrate on open). `index_brief` takes the three new fields (eval_score defaults 1.0 for backward-compat). `find_similar` gains `rank_by_eval_score` — pulls a `k*4` distance-candidate pool, re-ranks by eval_score desc (distance tie-break), returns top-k. `SimilarBrief` carries `eval_score` + `section_skeleton`. `memory/episodic.py::compute_run_eval_score` — a run's 0-1 composite (fraction of skill invocations that passed cleanly, advisory failures included). `agent.py` computes the score post-run, threads it + a section-heading skeleton into `index_brief`, and switches the drafter's RAG from top-3-by-distance to **top-5-by-eval-score**. `skills/draft_content_brief.py` slims the few-shot payload to `[eval_score] angle + section skeleton` (~200 tokens each, not the ~1.5k-token full bodies). 9 new semantic-memory tests; 165 total green; all 30 regression cassettes re-recorded (the drafter-prompt change tripped them — intentional). **Note:** the 3 live cassettes (Notion/Stripe/Webflow) reverted to bootstrap during the all-fake re-record; re-record them live once v3's drafter-prompt changes settle (~chunk 8).
- [x] **Chunk 2 — Meta-agent skeleton.** `meta/edit_surface.py` — deny-by-default path validator (`ALLOWED_PREFIXES` = `skills/prompts/`, `evals/rubrics/`, `evals/proposed/`; explicit `DENIED_PREFIXES`; rejects traversal + absolute paths). `meta/analyze.py` — rule-based, **read-only** (`mode=ro` URI), deterministic pattern detection over v2's existing drift + judge-divergence signals; severity-ranked; skips patterns a rejected/inconclusive proposal targeted in the last 30 days. `meta/propose.py` — the single LLM call; `_build_context` carries the pattern + the affected skill's *actual outputs* but **never rubric prose** (anti-Goodhart); validates `edit_paths` against the edit surface before returning. `meta_proposals` table + `EpisodicMemory.record_meta_proposal`. `meta/run.py` — `run_meta_agent` orchestration + `python -m meta.run --dry-run` emits proposal Markdown (PR opening deferred to chunk 4 — non-dry-run raises `NotImplementedError`). 38 new meta tests; 203 total green; ruff clean. Verified `analyze` against the real demo DB (finds 2 drift patterns).
- [x] **Chunk 3 — The 6 meta-evals.** `meta/meta_evals.py` — `ProposedChange` input + `MetaEvalResult`, and the 6 reward-hacking checks: (1) `protected_path_check` — edit-surface validation + `_rubric_threshold_decreases` parser (flags a lowered numeric threshold OR a softened directive MUST→SHOULD; tightening passes); (2) `trivial_eval_check` — importlib-loads each `evals/proposed/*.py`, runs it against the protected corpus, rejects always-pass (no signal) AND always-fail (blocks healthy work); (3) `divergence_regression_check` — judge-vs-human divergence must not rise; (4) `cassette_integrity_check` — `regression_dataset/` untouched; (5) `verbosity_guard_check` — brief-length growth >15% needs a human-rating gain; (6) `single_change_check` — ≤1 file per proposal. `meta/baseline.py` + committed `meta/baseline.json` — sha256 of every rubric + denylisted eval module, `verify_baseline` detects tamper; a test asserts the committed snapshot stays in sync. `meta/fixtures/known_good/` + `known_bad/` (8 ContentBrief JSON each, protected corpus) + `tests/fixtures/proposed_evals/` (3 sample evals: discriminating / always-pass / always-fail). `evals/proposed/` package created (landing zone, not auto-discovered). 24 new tests; 227 total green; ruff clean.
- [x] **Chunk 4 — PR opening + CI gate.** `meta/github_pr.py` — `branch_name` (`meta-agent/<slug>-<YYYYMMDD>`, unit-tested) + `build_pr_body` (proposal Markdown + anti-rubber-stamp reviewer checklist) + `PRPublisher` Protocol + `GitHubPRPublisher` (real: git branch → `git apply -` the diff from stdin → commit → push → PyGithub `create_pull`; PyGithub lazy-imported so base install + tests never need it). `meta/run.py` non-dry-run path wired: `run_meta_agent(dry_run=False, publisher=...)` opens a PR and records a `meta_proposals` row (`status='proposed'`, branch, pr_number); `main()` builds a `GitHubPRPublisher` from `GITHUB_META_AGENT_TOKEN` + `GITHUB_REPO`. `meta/gate.py` — `proposed_change_from_git_diff` (parses `git diff origin/master...HEAD`, flags `evals/proposed/*.py` as new evals) + `run_gate` (runs the 4 diff-decidable meta-evals; checks 3 & 5 wired once the production-replay harness lands in chunks 7-8) + `python -m meta.gate` exit-code entry point. `.github/workflows/meta-agent-gate.yml` — runs on `meta-agent/*` PRs, restores `meta/` from the base ref first so the gate runs the trusted copy. `docs/meta_agent_pr_template.md` reviewer guide. `meta` extra in `pyproject.toml` (`PyGithub==2.5.0`). 9 new tests; 236 total green; ruff clean.

## Stretches
- [x] **Chunk 5 — Winning patterns.** `winning_patterns` table (append-only; one row per periodic extraction). `EpisodicMemory.record_winning_patterns` / `get_latest_winning_patterns` (decodes `patterns_json` → list). `SemanticMemory.top_scoring_briefs(limit)` — globally highest-scoring indexed briefs by eval_score. `evals/winning_patterns.py::extract_winning_patterns` — Sonnet call over the top-N high-scorers' angle + section-skeleton, distills 3-6 structural patterns, caches a row; no-op when nothing is scored yet. `geoquery extract-patterns [--top-n]` CLI command. Drafter: `DraftBriefInputs.winning_patterns` + a "Winning patterns" guidance block in `build_user_message`; `agent.py` threads `get_latest_winning_patterns()` in. `meta/analyze.py` gains `_winning_patterns_stale` — fires only when a *prior* extraction is >14 days old (never-extracted = unadopted, not stale; severity capped at 0.4 so real regressions always outrank it). 16 new tests; 248 default + 25 regression_full green; ruff clean; all 30 regression cassettes re-recorded (drafter-prompt change tripped them — intentional). **Note:** live cassettes (Notion/Stripe/Webflow) again reverted to bootstrap in the all-fake re-record — still deferred to ~chunk 8.
- [x] **Chunk 6 — Inner-loop revision strengthened.** `skills/base.py`: `judge_consensus_threshold` ClassVar (default 0.5) — a deterministic eval failing always gates; judges are consensus-gated, so a *lone* judge failing stays advisory but a *majority* failing fires a revision. Full-critique revision header — when a revision fires, *every* failing critique (deterministic + every failing judge, even non-gating ones) goes into the header, not just the gating ones. `SkillEscalation(RetryExceeded)` — `run()` catches retry-cap exhaustion and re-raises it carrying every attempt's failures + the final rejected output. `escalations` table + `EpisodicMemory.record_escalation` / `get_escalations`. `agent.py` catches `SkillEscalation` and writes a rich escalation row before aborting `aborted_retries`. `meta/analyze.py` gains `_escalation_patterns` — a skill escalating ≥2× in 14 days is a high-severity cluster signal (0.5 + 0.1·n). 11 new/updated tests; 255 default + 25 regression_full green; ruff clean. No prompt changes → regression cassettes untouched.
- [ ] **Chunk 7** — Outcome prediction (Opus 30-day-outcome judge, sampled subset).
- [ ] **Chunk 8** — Outcome feedback blend into `eval_score`.
- [ ] **Chunk 9** — Cron + measurement + auto-revert.
- [ ] **Chunk 10** — Learning-curve dashboard page (Plotly).
- [ ] **Chunk 11** — `SELF_IMPROVEMENT.md` + README v3 + final docs.
